// This file ensures that MIME types are correctly set for Vercel deployment
// It's loaded by the index.html file to validate that JavaScript is being served correctly

console.log('Vercel configuration helper loaded - checking MIME types');

// Export a simple function to verify JavaScript module loading
export function verifyModuleLoading() {
  return true;
}

// This script can be imported as a module to test if MIME types are correct